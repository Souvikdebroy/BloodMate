<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.blood02">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Signup" />
        <activity android:name=".MainActivity2" />
        <activity android:name=".Donate" />
        <activity android:name=".DonerList" />
        <activity android:name=".MyAccount" />
        <activity android:name=".RequestReceived" />
        <activity android:name=".RequestSend" />
        <activity android:name=".Needy" />
        <activity android:name=".AuthPhno" />
        <activity android:name=".Login"></activity>
    </application>

</manifest>